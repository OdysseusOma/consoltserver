<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/form.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <script src="https://cdn.tiny.cloud/1/no-api-key/tinymce/6/tinymce.min.js" referrerpolicy="origin"></script>
    <script>
      tinymce.init({
        selector: 'textarea'
      });
    </script>
</head>
<body>
    <div class="navHead">
        <div class="imageBox"><a href="/"><img src="/logo.png" alt="" class="imageimage"></a></div>
        <div class="webLinkBox"><a class='webLink' href="https://tryconsolt.netlify.app/">Go to website</a></div>
    </div>
    <div class="formfield">
        <h1 class="titletext">Edit Post</h1>
        <div class="formwrap">
            
            <form action="/user/<%= user.id %>?_method=PUT" method="post" enctype="multipart/form-data">
                <label for="title">Title</label>
                <input type="text" name="title" id="title" placeholder="Title" class="input" value="<%= user.title %>" required>
                
                <label for="Author">Author</label>
                <input type="text" name="author_name" id="author_name" placeholder="Author" class="input" value="<%= user.author_name %>" required>
                
                <label for="image">Image</label>
                <input type="file" name="image" id="image" class="input">
                
                <label for="post_length">Post Length</label>
                <input type="text" name="post_length" id="post_length" placeholder="Length (How many minutes to read) e.g. 10" class="input" value="<%= user.post_length %>" required>
                
                <label for="article">Post Content</label>
                <textarea type="text" name="article" id="article" placeholder="Article" class="input"><%= user.article %></textarea>
  
                <a href="/">Cancel</a>
                <button type="submit" class="btnsubmit">UPDATE POST</button>
                <div class="message">
                    <div class="success" id="success">Successfully updated</div>
                    <div class="failure" id="failure">Field can't be empty</div>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
